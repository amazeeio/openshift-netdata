apiVersion: v1
kind: Service
metadata:
  name: netdata-master
  namespace: netdata
spec:
  ports:
    - port: 19999
      protocol: TCP
      targetPort: 19999
  selector:
    service: netdata-master
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
